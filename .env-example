# Example .env file
# Rename this file to .env and fill in the values as needed.

# PostgreSQL database connection URL
# Format: host=<host> user=<user> password=<password> dbname=<dbname> sslmode=<sslmode>
# DATABASE_URL=host=localhost user=myuser password=mypass dbname=mydb sslmode=disable